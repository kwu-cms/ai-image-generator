# Stability AI API移行 - 作業タスクリスト

## 前提条件の確認（⚠️ 最優先）

### 1. Stability AI APIの調査 ✅
- [x] Stability AI APIの公式ドキュメントを確認
- [x] APIキーの取得方法を確認
- [x] 課金体系とレート制限を確認
- [x] サポートされているモデルと機能を確認
- [x] img2img、inpaintingの仕様を確認

### 2. Stability AI側の準備作業（⚠️ 実装前に必須）
- [ ] **Stability AIアカウントの作成**
  - https://platform.stability.ai/ にアクセス
  - アカウント登録とメール認証

- [ ] **プランの選択と契約**
  - 料金プランの確認
  - 必要なクレジット数の見積もり
  - クレジットの購入（Pay-as-you-goまたはサブスクリプション）

- [ ] **APIキーの取得**
  - https://platform.stability.ai/account/keys でAPIキーを生成
  - APIキーを安全に保存（一度しか表示されない）

- [ ] **クレジット残高の確認**
  - ダッシュボードで残高を確認
  - API経由でも確認可能: `GET /v1/user/balance`

- [ ] **テスト実行**
  - APIキーでアカウント情報を取得できるか確認
  - 簡単な画像生成テストを実行

### 3. 環境準備
- [ ] **ローカル環境の設定**
  - `.dev.vars`に`STABILITY_AI_API_KEY`を追加

- [ ] **本番環境の設定**
  - `wrangler secret put STABILITY_AI_API_KEY`で設定
  - 環境変数が正しく読み込まれるか確認

**詳細な手順**: `docs/STABILITY_AI_API_SETUP.md`を参照

## 工程1: データベーススキーマの変更

### 1.1 マイグレーションファイルの作成
- [ ] `migrations/0005_rename_images_to_generations.sql` を作成
  - `images`テーブルを`generations`にリネーム
  - `prompt` → `final_prompt`にカラム名変更
  - `generation_options` → `generation_settings`にカラム名変更
  - カラムの型と制約を確認

- [ ] `migrations/0006_extend_generation_reference_images.sql` を作成
  - `generation_reference_images`テーブルに`r2_object_key`を追加
  - `generation_reference_images`テーブルに`image_hash`を追加
  - `weight`カラムを追加（将来的な拡張用）

### 1.2 既存データの移行
- [ ] 移行スクリプトの作成
  - `prompt` → `final_prompt`へのデータ移行
  - `generation_options` → `generation_settings`へのデータ移行
  - `generation_reference_images`に`r2_object_key`と`image_hash`を追加

- [ ] 移行スクリプトのテスト
  - ローカル環境でテスト
  - データの整合性を確認

### 1.3 マイグレーションの実行
- [ ] ローカル環境でのマイグレーション実行
- [ ] リモート環境でのマイグレーション実行
- [ ] マイグレーション後のデータ確認

## 工程2: Workers側の実装変更

### 2.1 Stability AI APIクライアントの実装
- [ ] Stability AI SDKのインストール確認
  - または、fetch APIを使用した実装

- [ ] API呼び出し関数の実装
  - `callStabilityAIAPI()`関数の作成
  - 認証ヘッダーの設定
  - リクエストボディの構築

- [ ] エラーハンドリングの実装
  - レート制限エラーの処理
  - クレジット不足エラーの処理
  - ネットワークエラーの処理

### 2.2 参照画像の取得とエンコード
- [ ] R2からの画像取得関数の実装
  - `getReferenceImageFromR2()`関数の作成
  - エラーハンドリング

- [ ] 画像のエンコード処理
  - base64エンコードの実装
  - メモリ制限の考慮（サイズチェック）

- [ ] 複数画像の処理
  - 画像配列の処理
  - メモリ使用量の監視

### 2.3 プロンプト生成ロジックの変更
- [ ] `enhancePromptWithRoles()`関数の確認
  - 既存のロジックを維持
  - 必要に応じて微調整

- [ ] 最終プロンプトの生成
  - 役割情報の埋め込み
  - DBへの保存

### 2.4 品質設定の固定化
- [ ] 品質プリセットの定義
  - `QUALITY_PRESETS`定数の作成
  - 標準品質と高品質のプリセット

- [ ] 設定の適用ロジック
  - フロントエンドからの`generationOptions`を無視
  - Workers側で固定設定を適用

### 2.5 エラーハンドリングの強化
- [ ] 4段階のエラーハンドリング実装
  1. 参照画像保存段階
  2. 外部API呼び出し段階
  3. 出力画像保存段階
  4. DB記録段階

- [ ] 構造化されたエラーメッセージ
  - `stage`フィールドの追加
  - `details`フィールドの追加
  - `retry_after`フィールドの追加（レート制限時）

### 2.6 `handleGenerate()`関数の変更
- [ ] OpenAI API呼び出しをStability AI API呼び出しに置き換え
- [ ] 参照画像の取得とエンコード処理を追加
- [ ] img2imgとtext-to-imageの判定ロジック
- [ ] レスポンス処理の変更

### 2.7 R2ストレージ構造の変更
- [ ] ディレクトリ構造の変更
  - `images/` → `generated-images/`
  - `reference-images/`は維持

- [ ] 既存コードの更新
  - パスの変更箇所を特定
  - 一括置換

## 工程3: フロントエンドの調整

### 3.1 APIリクエスト構造の確認
- [ ] 既存のUI構造を確認
- [ ] 必要に応じて微調整
- [ ] エラーメッセージ表示の確認

### 3.2 エラーメッセージの表示
- [ ] 段階別のエラーメッセージ表示
  - `stage`に応じたメッセージ表示
  - `details`の表示

- [ ] リトライ機能の追加（将来的な拡張）
  - UIの準備（現時点では実装しない）

## 工程4: テストと動作確認

### 4.1 単体テスト
- [ ] Stability AI API呼び出しのテスト
  - text-to-imageのテスト
  - img2imgのテスト

- [ ] エラーハンドリングのテスト
  - 各段階でのエラー処理
  - エラーメッセージの確認

### 4.2 統合テスト
- [ ] 参照画像を使った生成のテスト
  - 1枚の参照画像
  - 複数の参照画像
  - 異なる役割ラベルの組み合わせ

- [ ] 履歴表示のテスト
  - 生成履歴の表示
  - 参照画像の表示

### 4.3 パフォーマンステスト
- [ ] メモリ使用量の確認
  - 参照画像のサイズと枚数の上限確認
  - Workersのメモリ制限内で動作するか確認

- [ ] レスポンス時間の確認
  - API呼び出し時間
  - 画像処理時間

### 4.4 負荷テスト
- [ ] 同時リクエストのテスト
- [ ] レート制限の確認
- [ ] エラーハンドリングの確認

## 工程5: ドキュメントの更新

### 5.1 READMEの更新
- [ ] Stability AI APIへの移行を記載
- [ ] 新しい機能の説明を追加

### 5.2 セットアップガイドの更新
- [ ] Stability AI APIキーの設定手順を追加
- [ ] マイグレーション手順を追加

### 5.3 API仕様書の更新
- [ ] リクエスト/レスポンス形式の更新
- [ ] エラーメッセージの仕様を追加

## 工程6: 運用準備

### 6.1 監視とアラート
- [ ] Stability AI APIの使用量監視
- [ ] エラー率の監視
- [ ] アラート設定

### 6.2 バックアップとロールバック計画
- [ ] マイグレーション前のバックアップ
- [ ] ロールバック手順の準備

### 6.3 段階的なロールアウト
- [ ] テスト環境での動作確認
- [ ] 本番環境への段階的な適用

## 優先順位

### 高優先度（必須）
1. Stability AI APIの調査と準備
2. データベーススキーマの変更
3. Workers側の実装変更（API呼び出し部分）
4. 基本的な動作確認

### 中優先度（重要）
1. エラーハンドリングの強化
2. 品質設定の固定化
3. 統合テスト

### 低優先度（改善）
1. パフォーマンス最適化
2. ドキュメントの更新
3. 監視とアラート

## 注意事項

1. **既存機能の維持**
   - 既存の生成履歴は保持
   - 既存のUI構造は維持
   - 後方互換性を考慮

2. **段階的な実装**
   - まずは基本的な移行を完了
   - その後、機能拡張を追加
   - 既存機能を壊さないように注意

3. **テストの徹底**
   - 各工程で十分なテストを実施
   - 本番環境への適用前に必ずテスト

4. **メモリ制限の考慮**
   - Workersのメモリ制限（128MB）を常に意識
   - 参照画像のサイズと枚数に上限を設ける

5. **課金管理**
   - Stability AI APIの課金体系を理解
   - 品質設定を固定化して予算を管理
   - 使用量の監視
